import type { NextPage } from 'next';
import Head from 'next/head';

const Home: NextPage = () => {
  return (
    <div>
      <Head>
        <title>Claudio Freitas</title>
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <main>Claudio Freitas</main>
    </div>
  );
};

export default Home;
